import {getSettings,json,todayKST} from './_shared.js';
export default async(req)=>{if(req.method!=='POST')return json({error:'Method not allowed'},405);try{const {code}=await req.json();if(!/^\d{4}$/.test(code||''))return json({ok:false,message:'입장코드는 4자리 숫자입니다.'},400);const s=await getSettings(),now=todayKST();if(code!==s.accessCode&&code!==s.instructorCode)return json({ok:false,message:'입장코드를 확인해주세요.'},401);if(now<s.startDate)return json({ok:false,message:'아직 사용기간이 아닙니다.'},403);if(now>s.endDate)return json({ok:false,message:'사용기간이 종료되었습니다.'},403);return json({ok:true,role:code===s.instructorCode?'instructor':'user'})}catch(e){console.error(e);return json({ok:false,message:'입장 확인 중 오류가 발생했습니다.'},500)}};
